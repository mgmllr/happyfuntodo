<h1>Editing item</h1>

<%= form_for [@list, @item] do |f| %>

    <div class="field">
      <%= f.text_field :task, :value => @item.task %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>

<%= link_to 'Back', list_path(@list) %>
